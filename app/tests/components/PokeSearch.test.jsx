var React = require('react');
var ReactDOM = require('react-dom');
var expect = require('expect');

var PokeSearch = require('PokeSearch');

describe('PokeSearch', () => {
  it('should exist', () => {
    expect(PokeSearch).toExist();
  });
});
