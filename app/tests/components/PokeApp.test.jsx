var React = require('react');
var ReactDOM = require('react-dom');
var expect = require('expect');

var PokeApp = require('PokeApp');

describe('PokeApp', () => {
  it('should exist', () => {
    expect(PokeApp).toExist();
  });
});
