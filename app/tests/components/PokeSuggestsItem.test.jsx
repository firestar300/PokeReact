var React = require('react');
var ReactDOM = require('react-dom');
var expect = require('expect');

var PokeSuggestsItem = require('PokeSuggestsItem');

describe('PokeSuggestsItem', () => {
  it('should exist', () => {
    expect(PokeSuggestsItem).toExist();
  });
});
