var React = require('react');
var ReactDOM = require('react-dom');
var expect = require('expect');

var PokeSuggests = require('PokeSuggests');

describe('PokeSuggests', () => {
  it('should exist', () => {
    expect(PokeSuggests).toExist();
  });
});
